server:
  port: 8080
  error:
    whitelabel:
      enabled: false
spring:
  application:
    name: socshared-microservice-template
  profiles:
    active: ${SPRING_PROFILE:dev}

jwt:
  secret: ${SECRET_JWT_KEY}
  access_token:
    expired: 1800000
  refresh_token:
    expired: 2592000000
